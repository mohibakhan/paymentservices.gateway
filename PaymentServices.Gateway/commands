services.AddCosmosContainer(config, 
    config["app:AppSettings:COSMOS_TRANSACTIONS_CONTAINER"] ?? "tchSendTransactions", 
    "transactions");
services.AddCosmosContainer(config, 
    config["app:AppSettings:COSMOS_IDEMPOTENCY_CONTAINER"] ?? "tchSendIdempotency", 
    "idempotency");
